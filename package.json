{
  "name": "fork-in-herdr",
  "version": "0.1.0",
  "description": "omp and pi plugin: /fork-in-herdr tab-forks the current herdr tab with the agent session",
  "omp": {
    "extensions": ["./src/omp.ts"]
  },
  "pi": {
    "extensions": ["./src/pi.ts"]
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/bun": "^1.3.0"
  }
}
